<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.personal.chatroommobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatRoomMobile"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">
        <!-- Auth activity -->
        <activity
            android:name=".ui.auth.AuthActivity"
            android:exported="true"
            android:label="@string/label_auth_activity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/label_main_activity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ChatRoomMobile.NoActionBar" />
        <!-- Chat activity -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="true"
            android:label="@string/label_main_activity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ChatRoomMobile.NoActionBar" />
    </application>

</manifest>